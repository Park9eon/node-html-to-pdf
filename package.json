{
  "name": "capture",
  "version": "0.0.1",
  "scripts": {
    "start": "npm run chrome && npm run phantom",
    "chrome": "node capture_by_chrome.js",
    "phantom": "phantomjs capture_by_phantom.js"
  },
  "dependencies": {
    "phantomjs": "^2.1.7",
    "puppeteer": "^1.0.0"
  }
}
